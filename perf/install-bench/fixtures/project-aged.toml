[package]
name = "perf-bench-project"
version = "0.0.1"

# Same dependency set as project.toml, with 9 pins one release behind.
# `mops update` moves every aged pin back to the project.toml version
# without crossing a caret bound.

[dependencies]
core = "2.5.0"
caffeineai-authorization = "1.0.0"
caffeineai-data-viewer = "0.1.0"
caffeineai-email = "0.1.2"
caffeineai-email-calendar-events = "0.1.1"
caffeineai-email-marketing = "0.1.1"
caffeineai-email-verification = "0.1.1"
caffeineai-http-outcalls = "0.1.3"
caffeineai-invite-links = "0.1.2"
caffeineai-lints = "0.1.0"
caffeineai-object-storage = "1.1.1"
caffeineai-oql = "0.4.0"
caffeineai-stripe = "0.1.2"
caffeineai-user-approval = "0.1.1"
google-oauth = "0.2.1"
googlecalendar-client = "0.1.4"
googlemail-client = "0.1.5"
openai-client = "0.2.4"
x-client = "0.2.4"
slack-client = "0.0.1"
spotify-client = "0.2.2"
tmdb-client = "0.1.1"
twilio-client = "0.1.1"
quickbooks-client = "0.1.0"
weatherapi-client = "0.1.1"
