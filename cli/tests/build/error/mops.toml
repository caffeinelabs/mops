[toolchain]
moc = "1.3.0"

[dependencies]
core = "1.0.0"

[canisters]
foo = "src/Foo.mo"
bar = { main = "src/Bar.mo", args = ["--incremental-gc"], candid = "candid/bar.did" }

[build]
args = ["--release"]
