[dependencies]
core = "1.0.0"

[canisters]
foo = "src/Foo.mo"

[canisters.bar]
main = "src/Bar.mo"
args = ["--incremental-gc"]
candid = "candid/bar.did"
initArg = "(\"Custom text\")"

[build]
args = ["--release"]
